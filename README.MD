📅 Work Calendar Generator (FastAPI)
Веб-приложение на FastAPI, которое позволяет выбрать год и страны, после чего генерирует Excel-файл с рабочими днями, выходными и государственными праздниками, автоматически окрашивая их разными цветами.
Приложение использует внешний API (Calendarific) для получения национальных праздников выбранных стран.

📂 Структура проекта
app/
│-- main.py              # Основной веб-сервер (FastAPI)
│-- pet.py               # Логика получения данных и генерации таблицы
│-- templates/
│     └── index.html     # HTML-шаблон интерфейса
│-- static/
│     └── style.css      # Стили оформления
│-- test.py              # Минимальный тест
│-- requirements.txt     # Зависимости
│-- Dockerfile           # Docker-сборка
│-- README.md

🚀 Возможности приложения
 • Ввод года.
 • Выбор одной или нескольких стран.
 • Получение данных о праздниках через API Calendarific.
 • Формирование Excel-таблицы:
 ◦ work day — рабочий день (красный)
 ◦ day off — выходной (серый)
 ◦ holiday — государственный праздник (зелёный)
 • Скачивание файла work_calendar_<год>.xlsx.
 • История введённых пользователем данных (с возможностью очистки).
 • Веб-интерфейс на основе Jinja2 + CSS.

🛠 Технологии
 • FastAPI
 • Jinja2
 • Pandas
 • OpenPyXL
 • Requests
 • Docker
 • Calendarific API

📥 Установка и запуск
1. Клонирование проекта
git clone <repository-url>
cd app
2. Установка зависимостей
pip install -r requirements.txt
3. Запуск приложения
uvicorn main:app --reload
Приложение будет доступно по адресу
http://127.0.0.1:8000/

🐳 Запуск через Docker
1. Сборка контейнера:
docker build -t work-calendar .
2. Запуск контейнера:
docker run -p 8000:8000 work-calendar

📘 Основные файлы
main.py
 • Создаёт FastAPI-приложение.
 • Показывает форму (GET /).
 • Обрабатывает отправку формы (POST /submit).
 • Генерирует Excel через функции из pet.py.
 • Ведёт список результатов в памяти.
pet.py
Содержит ключевую бизнес-логику:
 • fetch_public_holidays(...) — получает праздники из Calendarific.
 • build_table(...) — создаёт таблицу Pandas.
 • apply_colors(...) — раскрашивает ячейки Excel.
 • Словари:
 ◦ COUNTRY_CODES — ISO-коды стран.
 ◦ COUNTRY_MPID — MPID-коды.
 • Поддерживаемые страны:
Грузия, Беларусь, Азербайджан, Кыргызстан, Армения, Казахстан, Таджикистан, Узбекистан.
style.css
Готовая адаптивная стилистика формы и отображения результатов.
test.py
Базовый тест проверки работы функции build_table().

🧪 Запуск тестов
pytest

📄 Формат итогового Excel-файла
Имя файла:
work_calendar_<год>.xlsx

Столбцы:
 1 Country
 2 MPID
 3 Даты за весь год: 01.01.2026, 02.01.2026, ...
Цвета ячеек:
Состояние   Цвет
holiday     зелёный
day off     серый
work day    красный

🌐 Используемый API
Calendarific:
https://calendarific.com/
В проекте используется API-ключ (вставлен в код).

📝 Пример пользовательского сценария
 1 Пользователь открывает страницу /.
 2 Вводит год (например, 2026).
 3 Отмечает страны (например, Грузия, Казахстан).
 4 Нажимает кнопку «Сгенерировать».
 5 Приложение:
 ◦ Получает праздники.
 ◦ Формирует Excel.
 ◦ Раскрашивает ячейки.
 6 Файл появляется в корне проекта.